mcp>=1.2
numpy>=1.24
# Datasets are read through Mirage's GitHub resource (GitHub-as-virtual-disk).
mirage-ai
# Serverless/stateless ontology mode materializes the index into Redis (24h TTL).
redis>=5
